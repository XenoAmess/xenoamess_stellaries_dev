# 仓库工作规则

## 文档先行

- 本项目采用文档先行：任何功能、测试夹具、自动化工具或 Mod 代码改动，都必须先在 `docs` 中记录目标、范围、设计方案与验收标准，再开始实施。
- 实施期间发现的新事实、风险和经验教训应实时回写 `docs`；代码和文档不一致时，任务不视为完成。
- 只进行只读调查或执行既有测试方案时，可以直接操作，但调查结论与测试证据必须沉淀到 `docs`。

## Git 工作流

- 完成用户吩咐的任何任务后，默认立即提交本次任务相关的改动，并将提交推送到当前分支所跟踪的远端分支。
- 除非用户明确要求不提交或不推送，否则不需要再次征求确认。
- 提交时只包含当前任务相关的文件，不得擅自提交或覆盖用户的其他未完成改动。

## 项目与知识库

- 本仓库是 Stellaris 综合管理项目；本仓库的 `docs` 目录是项目文档知识库。
- `D:\workspace\ck3_eternal_recurrence` 是 CK3 综合管理项目。可以查阅其中的 `docs` 目录，学习 Paradox 脚本语言的基础规则和共性知识。
- CK3 项目文档描述的是 CK3 方言，不得未经验证就把 CK3 特有语法当作 Stellaris 语法使用。
- 学到或确认 Stellaris 特有的语法规则时，必须记录到本仓库的 `docs` 目录。
- 学到或确认 Paradox 游戏共通的规则时，除记录当前项目所需内容外，还必须更新 `D:\workspace\ck3_eternal_recurrence\docs` 中的相应文档，并在该仓库提交和推送相关改动。

## P 语言 Mod 验收

- `D:\workspace\open_kaishek` 是 P 语言引擎验收工具项目。
- 后续验收任何 P 语言 mod 时，必须先使用该工具执行验收检查，并遵循该工具仓库中的使用说明。
- 只有通过该工具的验收后，才能宣告 mod 验收完成；如果工具无法运行或验收未通过，必须明确报告阻塞原因或检查结果。
- 如果验收过程中发现该工具存在缺陷或缺少完成验收所需的功能，必须暂停 mod 验收，先在 `D:\workspace\open_kaishek` 中修复或完善工具并验证修复，然后再继续验收 mod。
- 对该工具产生的修复或功能完善，必须在 `D:\workspace\open_kaishek` 仓库中提交并推送到其远端；提交时只包含该工具任务相关的改动，并遵循该仓库自身的规则。

## 语言与实机验收范围

- 本项目后续所有 Stellaris 实机、UI、OCR、存档重载和运行时回归只使用简体中文（`l_simp_chinese`）。
- 英文及其他语言仍按需求提供翻译，但只做静态验收，不为这些语言启动游戏，也不把它们纳入实机回归矩阵。
- 非中文翻译的静态验收至少覆盖本地化键完整性、文件头与编码、引用一致性、原始 key 泄漏风险，以及不应出现的简体中文占位文本。
- 报告非中文翻译结果时，必须明确写为“静态校验通过”或“运行时不在范围内”，不得表述为已经通过实机测试。

## Mod 版本与 Steam 发布

- 本 Mod 使用独立于 Stellaris 游戏版本的语义化版本号（SemVer）；根目录 `VERSION` 是唯一版本源，`descriptor.mod` 的 `version` 必须与其完全一致，游戏兼容范围只由 `supported_version` 表达。
- 每次成功发布到 Steam 创意工坊都必须使用一个尚未发布的新 Mod 版本；已成功发布的版本号不得复用。失败后、尚未形成远端更新的重试可以沿用同一版本。
- 每次 Steam 发布前必须在根目录 `CHANGELOG.md` 建立该版本的正式条目，至少记录新增、变更、修复、兼容范围、验收情况和已知限制；不得先上传、后补 changelog。
- Steam 的 Change Note 必须以 `[v<版本号>]` 开头，并从同版本 changelog 条目提炼，不得维护一份内容相互矛盾的临时说明。
- 正式版发布前使用 `-rc.N` 等预发布版本；除非用户明确要求，不得把预发布版本上传到公开创意工坊。正式发布后必须核验远端内容，再创建并推送同名 Git 标签 `v<版本号>`。
