# 合成女王替换·牢大：首次 Steam 工坊发布计划

日期：2026-09-25。用户已明确要求将独立 Mod **XenoAmess的合成女王替换·牢大** 发布到 Steam，并把实际游玩截图作为图片资产上传。本物品须通过 Steamworks `CreateItem` 新建；既有合成女王美化物品 `3807768508` 和只读上游 `3710613857` 均不得更新。

## 目标、内容与版本

- 本 Mod 的首次公开版本定为 `0.1.1`。仓库全局 `v0.1.0` 标签已属于另一独立合成女王 Mod；为遵守“正式版本必须有同名 `v<版本>` 标签且不得移动既有标签”的仓库规则，本次使用尚未占用的 `v0.1.1`。发布前把本目录 `VERSION`、`mod/descriptor.mod`、`CHANGELOG.md` 和 Steam Change Note 统一到 `0.1.1`；`supported_version` 仍为 `4.5.*`。描述符发布前不含任何 `remote_file_id`。
- 正式内容继续仅覆盖原版 10 个合成女王肖像键，DDS 字节和已验收候选版保持一致。新加一张从最终透明肖像确定性合成的 `351×313` PNG 缩略图，描述符设置 `picture="thumbnail.png"`；不改变 P 语言肖像定义、玩法脚本或其它图片。
- 以隔离简体中文 Stellaris 4.5.1 实机的 Steam F12 原始截图 `after-restart.jpg`、`cetana-dialogue.jpg` 作为 `workshop/media/` 的两张图片。前者必须清楚显示“孩子们，我终于回到了你们的身边”，并同时作为工坊**主预览图**和 BBCode 截图段落第一张；后者作为附加预览。图片须显示游戏对话、人物和原球衣广告；复制原始 JPEG 字节，不生成或重绘游戏画面。描述符用独立的 `thumbnail.png`，图片栏的主预览则用上述实机截图。
- `workshop/description.bbcode` 是完整页面说明真源。明确人物、球衣及冰红茶广告保留、EvoLink 透明通道、4.5.* 与《机械纪元》要求、同文件 Mod 冲突、静态肖像限制、其它剧情图片仍为原版，以及 R2/R4 未验收。正文用 `[img]` 引用固定 Git 提交的两张真实游玩截图，控制在 Steamworks 说明长度限制内。
- `workshop/change-note-v0.1.1.txt` 从正式 changelog 提炼并以 `[v0.1.1]` 开头。用户未要求复用既有物品，也未要求预发布版，因此公开上传正式 `0.1.1`，候选 `0.1.0-rc.1` 不上传。

## 门禁与操作顺序

1. 本计划落盘后再改 Mod、图片和发布工具。缩略图、截图、正式版本先提交推送，取得不可变 Git SHA；随后将该 SHA 写入 BBCode 图片 URL，再提交推送说明。检查 raw URL 返回对应 JPEG 字节、说明长度和所有文件哈希。
2. 先用 `C:\workspace\open_kaishek` 的 `stellaris-4.5.1` profile 验收正式 P 语言入口；检查 UTF-8 无 BOM、描述符解析、版本一致、DDS 与已通过实机的 SHA-256 一致，发布包只含四个白名单文件。因为肖像 DDS 与映射字节不变，沿用同一游戏画面实机证据；缩略图只做图片静态检查。
3. 参考已成功的 Steamworks UGC v016 发布工具，制作独立、默认只读的本 Mod 上传工具。上传前要求干净 Git 工作树、Steam 登录和 App ID `281990`；仅 `--publish` 才调用 `CreateItem`，立刻持久化新 ID，遇到法律协议标志立即停下。对新 ID 设置标题、完整 BBCode、内容目录、上述“孩子们”实机主预览、另一张附加截图、标签和公开可见性，最后用同版本 Change Note 提交。
4. 成功上传后，匿名读取新物品详情并核对新 ID、标题、公开状态、BBCode、图片数量与内容、Change Note；从空缓存重新下载并逐文件比对正式包。核对两个既有物品未被本次工具用作更新目标。将新 ID 和结果写回 docs、描述符及证据；远端已成功发布的 `0.1.1` 不得复用作任何后续更新。
5. 回写与核验提交推送后，创建并推送 `v0.1.1` 标签；不移动已有 `v0.1.0`。另加产品别名标签以便区分两个 Mod。

## 已知验收边界

候选版 `open_kaishek` 静态校验、透明素材、R1 启动、R3 中文对话、R5 保存后重启载入通过。R2 基础肖像独立界面与 R4 合法结局上下文没有实际观察到；本次发布不宣称完整实机矩阵通过。所有工坊文案和更新日志必须如实披露。游戏安装目录保持只读；不上载参考照片、EvoLink 原始任务或运行日志到 Mod 内容包。

## 预期完成证据

新公开物品有独立 ID 和可访问页面；远端详情、说明、图片、变更说明及下载文件均与仓库正式版一致。`workshop/media/` 两张图与归档的 F12 截图逐字节相同，远端主预览及附加图片也与其一致。Steam 回执、匿名核验、下载比对、Git 提交和标签写入本计划及 `evidence/v0.1.1/`。

## 发布前静态检查记录

- 正式 `VERSION`、描述符和 changelog 均为 `0.1.1`；`supported_version="4.5.*"`。`mod/` 当前为描述符、肖像定义、DDS、缩略图四个文件，发布前没有远端 ID。
- DDS SHA-256 `a35c0431498c6b8511bed0604517ba629695edad86e0556c277019d0bbbbf071`、肖像定义 SHA-256 `70c05b267415576635831cef640283f7df3c1d1f719e1fd8ba444bd7323e51b4`，均与实机候选包完全相同。`open_kaishek` `validate --profile stellaris-4.5.1` 为 `VALIDATED`、0 语法/语义诊断；描述符 `parse` 为 `PARSED`、0 诊断、`roundTrip=true`。
- 新描述符缩略图 `351×313` PNG、83,701 字节。`01-children-returned.jpg` 与重启后 F12 截图逐字节相同，`2560×1440`、464,545 字节、SHA-256 `ff0a65dce629a69c1d2afcb1c1b35a84f57de8f619b2e9e9f8f50df4ec74fabf`；`02-first-dialogue.jpg` 与首轮 F12 截图逐字节相同，`2560×1440`、464,291 字节、SHA-256 `a3ab86f404718ec0a2259a152310eb816066922966bf5596eb9448022002a883`。第一张画面中的台词正是“孩子们，我终于回到了你们的身边”。
- 发布工具及图片制作工具 `py_compile` 通过；缩略图已目视检查人物、球衣及广告，游戏截图未做重绘或压缩转换。
- 正式包、两张图片与发布工具已由提交 `9d754e15718911f85b48a05f0541d9977ec4d930` 推送到 `origin/main`。BBCode 使用此不可变提交的图片 URL；两条 URL 均返回 `200 image/jpeg`，没有重定向，下载字节 SHA-256 与仓库截图相同。BBCode 为 2,248 UTF-8 字节，包含两组完整 `[img]` 标签。
