# Steam 创意工坊发布方案

## 目标与发布边界

- 创建一个全新的创意工坊物品；原物品 `3710613857` 仅作为只读上游来源，严禁更新或覆盖。
- v1.0.0 已作为隐藏的新物品创建基线；用户随后要求统一长期维护名称并补充原作、致谢与授权文案。为避免复用已经成功上传的版本号，首次公开版本推进为 Mod `1.0.1`。
- 新物品正式标题为 `无限岗位（XenoAmess维护版）`，避免把长期维护身份绑定在单个游戏版本上；标签沿用 `Balance`，预览图沿用仓库中的 `thumbnail.png`，初次发布后保持公开可见。
- 上传内容来自 `vivhite_infinite_positions/mod`；不得把测试工具、夹具、运行证据或仓库文档打进 Mod 包。
- 本文先冻结说明与变更说明草案。用户审核通过且发布门禁通过后，才允许对 Steam 执行上传。

## 原说明获取与保留策略

2026-09-06 通过 Steam 官方 `ISteamRemoteStorage/GetPublishedFileDetails/v1/` 读取物品详情，取得字段 `description`：

- 标题：`无限岗位`
- 发布者 SteamID：`76561198363589965`
- 源说明字符数：`1950`
- 源说明 UTF-8 字节数：`4796`
- 源说明 UTF-8 SHA-256：`ba71a4e28e48ff6b466c0615cd271e3f7c9b9e868373b04b08ecfe29dde2ac76`
- 源说明中的 BBCode 起始标签数：`0`

Steam 返回的上游源说明是纯文本，没有现存 BBCode 标签。新物品发布时不得用网页渲染文本、仓库摘要或重新撰写的简介替换它；最终说明必须按以下方式构造：

1. 发布前再次从官方接口读取 `description`。
2. 与上述 SHA-256 比对；若上游说明已变化，停止发布并重新生成审核稿。
3. 原说明逐字保留。
4. 在原说明末尾追加两个换行，再追加下方 BBCode 区块。
5. 上传新物品后读取新 ID 的远端说明，核对原文前缀、追加区块和整体哈希；同时确认上游物品的更新时间和内容均未改变。

## 拟追加的 BBCode

```bbcode
[h1]原作、致谢与授权[/h1]
[list]
[*][b]原 Mod：[/b][url=https://steamcommunity.com/sharedfiles/filedetails/?id=3710613857]无限岗位[/url]
[*][b]致谢：[/b]感谢原 Mod 作者的创作与维护劳动，本维护版以原作提供的玩法和内容为基础。
[*][b]授权说明：[/b]本维护版已获得原 Mod 作者授权进行二次开发与发布。
[*][b]项目 GitHub：[/b][url=https://github.com/XenoAmess/xenoamess_stellaries_dev/]源码与问题反馈[/url]
[/list]

[h1]Mod v1.1.0 更新补充｜Stellaris 4.4.6[/h1]
[i]本节是在原说明基础上的追加更新；若与上方旧版本说明冲突，以本节为准。[/i]

[h2]本次更新[/h2]
[list]
[*][b]官方语言完整支持：[/b]已补齐英语、巴西葡萄牙语、德语、法语、西班牙语、波兰语、俄语、简体中文、日语和韩语的完整本地化。
[*][b]支持方舟舰与游牧殖民地：[/b]计划 00—12 现在可以在方舟殖民地的决议界面中使用。
[*][b]新增折叠式决议菜单：[/b]默认只显示“展开岗位扩展计划”入口；展开后可以访问全部适用计划，并可随时收起。展开/收起状态会随存档保留。
[*][b]新增计划 13——扩建活体陈设安置区：[/b]仅失控机仆可见，用于扩充活体陈设岗位与住房。
[*][b]适配 Stellaris 4.4：[/b]兼容范围调整为 4.4.*，并修复旧描述符中无效 supported_version 产生的加载日志错误。
[/list]

[h2]计划 13 数值[/h2]
[list]
[*]消耗 1000 矿物，执行时间 180 天。
[*]+600 住房。
[*]+600 活体陈设岗位容量。
[*]该地块的活体陈设岗位效率 +10%。
[*]与原有岗位扩建计划一致，可以重复执行并叠加效果。
[/list]

[h2]使用与测试说明[/h2]
[list]
[*]原计划 00—12 的功能和数值保持不变；本次更新后总计 14 项岗位扩建计划。
[*]本 Mod 不会由 AI 主动执行决议。
[*]本版本在 Stellaris 4.4.6、简体中文、仅启用本 Mod 的环境中进行实机回归。
[*]10 种官方语言均有完整翻译；仅简体中文进行实机回归，其余 9 种语言只进行静态文本校验。
[*]第三方 UI Mod 可能改变决议菜单的显示或交互；遇到菜单问题时，请先在仅启用本 Mod 的环境中复现。
[/list]

[h2]折叠菜单实机证据（简体中文）[/h2]
[b]默认折叠：[/b]新殖民地只显示“展开岗位扩展计划”，计划列表不会占满决议面板。
[img]https://raw.githubusercontent.com/XenoAmess/xenoamess_stellaries_dev/a3b1952b389a0bfcd5fb1457a016c982f827118c/workshop/media/01_menu_default_collapsed.jpg[/img]

[b]展开后：[/b]入口切换为“收起岗位扩展计划”，并显示适用于当前殖民地的岗位扩建计划。
[img]https://raw.githubusercontent.com/XenoAmess/xenoamess_stellaries_dev/a3b1952b389a0bfcd5fb1457a016c982f827118c/workshop/media/02_menu_expanded.jpg[/img]

[b]再次收起：[/b]功能决议重新隐藏，只保留展开入口；该状态可随存档保持。
[img]https://raw.githubusercontent.com/XenoAmess/xenoamess_stellaries_dev/a3b1952b389a0bfcd5fb1457a016c982f827118c/workshop/media/03_menu_collapsed_again.jpg[/img]
```

## Steam 更新说明草案

该文本用于上传时独立的 Change Note 字段，不替代页面完整说明：

```text
[v1.0.1] 将维护版名称统一为“XenoAmess维护版”；工坊说明补充原 Mod 链接、对原作者的致谢、已获授权进行二次开发与发布的说明，以及折叠菜单的三张简体中文实机证据图。功能、数值和 Stellaris 4.4.* 兼容范围不变。
```

## 发布门禁

发布前必须全部满足：

1. 用户已下达发布指令；按本文件冻结的追加 BBCode 和 Change Note 执行。
2. `VERSION`、描述符、机器合同和 `CHANGELOG.md` 已同步推进为 `1.0.1`，Steam Change Note 与该版本 changelog 一致。
3. I1-003 简体中文反向可见性通过：普通帝国与非机仆机械帝国均看不到计划 13。
4. `open_kaishek` 对生产 Mod 的 P 语言入口全部 `VALIDATED`，仓库自动化测试通过。
5. 从干净 Git 提交构造上传目录，重新核对 Mod 树哈希、`descriptor.mod` 版本和文件清单；首次上传包不得携带上游 `remote_file_id="3710613857"`。
6. 在发布界面明确选择创建新物品，并记录 Steam 返回的新 Workshop ID；任何界面若显示将更新 `3710613857`，必须立即取消。
7. 上传完成后核对新物品 ID、标题、说明前缀、追加 BBCode、Change Note、更新时间及重新下载的文件内容，并确认上游 `3710613857` 未变化；随后把新 ID 写回描述符、文档和合同，创建并推送 Git 标签 `v1.0.1`。

## 两阶段发布顺序（2026-09-07 用户确认）

1. 先以当前冻结能力发布 `1.0.1`：更新隐藏物品 `3797257579` 的内容、标题、完整说明与 Change Note，从空缓存核验后改为公开，并完成 ID 回写与 `v1.0.1` 标签。
2. 使用 I1-002 已有的“仅启用本 Mod”简体中文实机证据关闭 `I1-R02`；不额外安装随机第三方 UI Mod，不承诺全量 UI Mod 兼容。
3. 继续完成 `I1-004` Stellaris 官方 10 种语言全量本地化、`I1-R01` 蜂巢岗位实机回归，以及综合夹具遗留项；9 种非中文语言仍只做翻译和静态验收。
4. 官方语言全量本地化属于向玩家新增完整语言能力，按 SemVer 将下一公开版本定为 `1.1.0`。完成对应 changelog、静态/中文实机门禁和发布稿后，再更新同一新物品并核验公开版本。

## 发布路径与登录阻塞记录

- 2026-09-07 在 Paradox Launcher `2024.14` 中创建了新的本地 Mod 条目 `mod/xenoamess_infinite_jobs_4_4`，并从仓库发布包覆盖其内容；覆盖后目录树 SHA-256 与仓库发布包同为 `a38f56da786e7e136ff306e8415f1a1f2fbd4191fddd4b49baf6d5f90ea53416`。
- 上传界面已明确选择 `Steam Workshop`，`Mod ID` 保持为空，说明字段使用“上游原文 + 两个换行 + 已审核 BBCode”；因此该次操作的意图是创建新物品，不存在更新上游 `3710613857` 的路径。
- 点击 `UPLOAD MOD` 后，Launcher 仍要求先登录 Paradox 账号。关闭登录框后，本地内外两份描述符均未出现 `remote_file_id`，说明本次尝试没有创建远端物品，也没有修改上游物品。
- 若没有可用的 Paradox 登录会话，允许改用 Stellaris 随附的 Steamworks UGC 接口，在当前已登录的 Steam 客户端会话中依次调用 `CreateItem`、设置标题/说明/内容/预览图/标签/公开可见性，再以本版本 Change Note 调用 `SubmitItemUpdate`。该回退路径仍必须满足“新建返回 ID、绝不传入上游 ID、发布后 API 核验及回写仓库”的全部门禁。
- Steamworks 首次创建若返回“需要接受 Workshop 法律协议”，必须停止公开发布，打开新物品的协议页面并由用户完成接受；不得代替用户同意法律条款。
- 用户提供可用 Paradox 账号后，Launcher 登录成功，并于 2026-09-07 创建全新物品 `3797257579`；成功页面与内外描述符写回的新 ID 共同证明上传完成。该物品创建后默认处于隐藏状态，公开前先订阅并从 Steam 缓存复核内容。
- 用户在公开前要求调整长期维护名称与归属说明，因此远端隐藏草稿暂不继续操作。仓库和本地上传 staging 已推进为 v1.0.1，二者 7 文件树 SHA-256 均为 `9b3e8ece4a6a93d529dd714e539dab9f7c7ee359b9b4df2e251accf24d0d7b7a`；外层 `.mod` 单独保留新物品 ID `3797257579`，待用户审阅文案后再上传。

## 工坊文案的仓库真源

- 完整工坊说明必须保存在 `workshop/description.bbcode`；该文件由“官方 API 返回的上游原说明逐字前缀 + 两个换行 + 本文已审核追加 BBCode”组成，是后续网页编辑和远端逐字核验的唯一真源。
- 仓库文本统一使用 LF；把上游返回的 CRLF 规范化为 LF 后，原说明前缀逐字符一致。v1.1.0 完整文件为 3,707 个 Unicode code point（3,711 个 UTF-16 code unit）、7,935 个 UTF-8 字节，SHA-256 为 `d369241bc666af8ea928c14f8f2111d0500ccd28a00ec44406dc9be193f9531f`，距 Steamworks 的 8,000 字节上限尚余 `65` 字节，且追加区块与本文代码块逐字符一致。
- 页面标题必须与 `descriptor.mod` 一致为 `无限岗位（XenoAmess维护版）`。
- 授权事实的项目内依据是用户于 2026-09-07 的明确确认；工坊文案必须同时包含原 Mod 可点击地址、对原作者劳动的感谢和已获授权进行二次开发与发布的说明。
- v1.0.1 文案与命名准备完成后，仓库自动化测试 `21/21` 通过；`open_kaishek` 的 Stellaris 4.4.6 profile 对 decisions、deposits、scripted trigger 三个生产入口再次全部返回 `VALIDATED`。当前长期 shell 继承了环境变量更新前的 `PATH`，本轮以机器级 `JAVA_HOME` 下的 `java.exe` 显式执行；机器级 `JAVA_HOME`、`MAVEN_HOME` 及用户 PATH 条目仍然存在。

## 折叠菜单实机图片物料

- v1.0.1 公开页必须增加三张简体中文实机证据：默认折叠、展开后显示功能决议、再次收起。源证据分别为运行 `20260906T053229Z` 的 `i1002-carrier-earth-default-valid.png`、`i1002-carrier-earth-expanded.png`、`i1002-carrier-earth-collapsed-final.png`。
- 发布副本保存为 `workshop/media/01_menu_default_collapsed.jpg`、`02_menu_expanded.jpg`、`03_menu_collapsed_again.jpg`。只允许保持原分辨率的确定性 JPEG 转码，不裁切、不重绘、不使用生成式图像修改；原始无损 PNG 继续留在运行证据目录。
- 三张 JPEG 必须各自小于 Steam 实测的 2 MB 限制，并上传到物品图片栏。工坊说明同时用 `[img]` 引用固定到包含图片字节之 Git commit 的 GitHub raw URL；提交前验证响应为 `200`、MIME 为 `image/jpeg`、无重定向且允许跨域。
- BBCode 中每张图必须带中文状态标题和一句断言，完整说明仍不得超过 Steamworks 的 8,000 UTF-8 字节上限。
- 2026-09-07 发布前复核：三个固定提交 URL 均直接返回 `200 image/jpeg`，`Access-Control-Allow-Origin: *`，长度分别为 `724333`、`725640`、`724706` 字节；完整说明为 `7,612` UTF-8 字节，距上限尚余 `388` 字节。

## v1.0.1 实际发布结果

- 2026-09-07 通过 Paradox Launcher 把 v1.0.1 更新到新物品 `3797257579`；上传表单自动识别的 Mod ID 为 `3797257579`，没有触及只读上游 `3710613857`。
- 工坊标题已更新为 `无限岗位（XenoAmess维护版）`。匿名 Steam 详情接口返回 `result=1`、`visibility=0`；远端完整说明为 `7,612` UTF-8 字节，SHA-256 `53c7f838dd2b9f328665b6cca7384fa8bd479c4ec69b0e14ca09e2a4c82d5b02`，与 `workshop/description.bbcode` 逐字一致。
- 三张简体中文折叠菜单实机图已依次上传到物品媒体栏，页面显示三个可切换缩略图；正文的三处 `[img]` 同时引用 Git 提交 `a3b1952b389a0bfcd5fb1457a016c982f827118c` 中的固定图片字节。
- 最新改动说明已经写入本文件冻结的 `[v1.0.1]` Change Note，工坊“改动说明”页可见完整文本。
- 删除本地旧缓存后，通过 Steam 控制台重新下载得到 7 个文件、总计 `630662` 字节。六个非描述符文件与仓库发布源逐字节一致；缓存描述符包含 `version="1.0.1"`、正确标题和 `remote_file_id="3797257579"`，`supported_version` 被 Launcher 规范化为 `v4.4.6`。
- 新 ID 回写后，仓库 Mod 树 SHA-256 为 `75753b3894d1288f37efaa151549b387507e36b9e7569c9b36959b07fc44f4c9`。匿名接口复核上游仍为标题 `无限岗位`、更新时间 `1783740925`、原始说明 SHA-256 `ba71a4e28e48ff6b466c0615cd271e3f7c9b9e868373b04b08ecfe29dde2ac76`，证明原物品未被修改。

## v1.1.0 发布稿与门禁

- 发布范围：在同一维护版物品 `3797257579` 上交付 Stellaris 官方 10 种界面语言的完整 60 键本地化；不修改计划 00—13 的玩法脚本、数值或存档合同。原物品 `3710613857` 继续保持只读。
- 运行时范围：只使用 `l_simp_chinese`。I1-R01 蜂巢岗位、S03/S04 计划 10 首次/重复执行、deposit 叠加和存档重载均已完成；9 种非中文语言只做键集合、文件头、编码、引用、数值和占位风险的静态校验。
- 页面说明：继续保持上游原说明逐字前缀、原 Mod 链接、致谢、授权说明、项目 GitHub 地址和三张简体中文折叠菜单证据；追加区块标题推进为 v1.1.0，并加入官方语言完整支持说明。
- 上传包：从干净 Git 提交构造，仅包含 `vivhite_infinite_positions/mod` 的 15 个文件；描述符版本必须为 `1.1.0`，远端 ID 必须为 `3797257579`。
- 发布后门禁：匿名详情接口确认 `visibility=0`、标题、完整 BBCode 和最新 Change Note；空缓存重新下载并核对全部语言文件、描述符与发布源；上游 `3710613857` 的更新时间和说明哈希必须保持不变。

Steam Change Note 冻结为：

```text
[v1.1.0] 补齐 Stellaris 4.4.6 官方支持的全部 10 种界面语言，每种语言均完整覆盖折叠菜单和计划 00—13 的 60 个本地化键。简体中文已完成实机回归，其他 9 种语言通过静态校验；玩法数值与 Stellaris 4.4.* 兼容范围不变。
```

## v1.1.0 实际发布结果

- 2026-09-07 通过从 Steam 启动的 Paradox Launcher `2026.11` 更新维护版物品；上传表单自动回填并复核 Mod ID `3797257579`，成功页明确返回“您的 Mod 已上传”。物品保持公开，匿名详情接口返回 `result=1`、`visibility=0`，标题仍为 `无限岗位（XenoAmess维护版）`。
- 远端完整说明包含原 Mod 地址、致谢、授权说明、项目 GitHub 地址 `https://github.com/XenoAmess/xenoamess_stellaries_dev/`、v1.1.0 更新说明和三张折叠菜单证据图。Steam 只去掉仓库真源末尾的一个 LF，因此远端为 `7,934` 个 UTF-8 字节，SHA-256 `882e4d25cb3635f46ad5a222bdebaaf95e30f3add05ad03743157f4a277da550`，与 `workshop/description.bbcode` 去掉末尾 LF 后逐字一致。
- Launcher 上传产生的最新构建时间为 `1788791259`，文件大小 `719938` 字节。Steam“改动说明”页的同时间条目已写入并公开读回本文冻结的完整 `[v1.1.0]` Change Note；Paradox Launcher 本身没有 Change Note 输入框，因此发布后通过物品所有者页面补录。
- 通过 Steam 控制台重新下载 manifest `2890725529084427628`，得到 15 个文件。十种本地化文件及其余 4 个非描述符文件均与仓库发布源逐字节一致；远端 `descriptor.mod` 正确包含 `version="1.1.0"`、`supported_version="4.4.*"`、维护版标题和 `remote_file_id="3797257579"`，唯一字节差异同样是 Steam 去掉末尾 LF。
- 仓库自动化测试 `28/28` 通过；`open_kaishek` Stellaris 4.4.6 profile 对 decisions、deposits、scripted trigger 三个生产入口全部返回 `VALIDATED`。运行时验收仍只声明简体中文通过，其他 9 种语言只声明翻译与静态校验通过。
- 匿名接口复核上游 `3710613857` 仍为标题 `无限岗位`、更新时间 `1783740925`、说明 SHA-256 `ba71a4e28e48ff6b466c0615cd271e3f7c9b9e868373b04b08ecfe29dde2ac76`，证明本次发布没有修改原物品。

## v1.2.0 发布方案与门禁

- 发布范围：在维护版物品 `3797257579` 上新增计划 14，并给居住站补齐计划 04—06、09、10 的对应区划容量；上游 `3710613857` 继续只读。
- 运行时门禁：run `20260910T010503Z` 已在 Stellaris 4.4.6、简体中文、仅启用本 Mod 的隔离环境通过。其他 9 种语言仍只声明静态校验通过。
- 页面说明：保留原说明、原 Mod 地址、致谢、授权说明、GitHub 地址和三张折叠菜单证据图，把追加更新区压缩并推进到 v1.2.0。
- 上传包：正式版本统一为 `1.2.0`，从干净 Git 提交构造 15 文件内容；上传前再次通过 `open_kaishek`、仓库测试、版本、树哈希和 BBCode 大小门禁。
- 发布后门禁：确认物品公开、标题、说明和改动说明；空缓存下载并比对发布源；上游物品保持不变。通过后创建并推送标签 `v1.2.0`。

Steam Change Note 冻结为：

```text
[v1.2.0] 新增计划 14“人口发展中心”：每次增加 600 住房、10% 人口增长速度和 10% 机械人口组装速度。计划 09/10 在居住站各增加 2 个能源/采矿区划容量；计划 04/05/06 在居住站各增加 2 个总区划槽位和 2 个研究区划容量。Stellaris 4.4.6 简体中文实机验收已覆盖效果、范围、重复叠加与存档重载；其余 9 种官方语言静态校验通过。
```

发布实测补充：Steam 客户端所有者页面拒绝保存 7,973 字节的 LF 文案；该表单可能按 CRLF 计入 8,000 字节限制。v1.2.0 因此进一步压缩历史更新说明，安全目标改为低于 7,600 个 UTF-8 字节，同时保留上游原文、归属授权、GitHub、新功能、语言边界和三张证据图。

## v1.2.0 实际发布结果

- 2026-09-10 通过 Paradox Launcher `2026.11.1` 更新维护版物品 `3797257579`；上传表单自动回填该 ID，成功页返回“您的 Mod 已上传”。匿名详情接口返回 `result=1`、`visibility=0`，标题仍为 `无限岗位（XenoAmess维护版）`。
- 远端构建时间为 `1789032403`，文件大小 `729113` 字节。
- 物品所有者页面已保存完整 v1.2.0 说明。Steam 返回的说明规范化为 LF 并去掉末尾换行后为 7,426 字节，SHA-256 为 `feef5333f929f03f19b401833d6c607ddc24cc155ca51aebb39e0e783ae2c6ee`，与仓库真源逐字一致；原 Mod 地址、致谢、授权、GitHub 和三张折叠菜单证据图均保留。
- 同一构建的最新改动说明已补录并公开读回，文本与本文冻结的 `[v1.2.0]` Change Note 完全一致。
- 上游物品 `3710613857` 的更新时间仍为 `1783740925`，文件大小仍为 `625141` 字节，确认本次发布没有修改原物品。
- 清空旧缓存后通过 Steam 控制台重新下载 manifest `893867195197576403`，得到 15 个文件。14 个非描述符文件与仓库发布源逐字节一致；远端描述符正确包含 `version="1.2.0"`、`supported_version="4.4.*"`、维护版标题和 `remote_file_id="3797257579"`，唯一字节差异是 Steam 去掉末尾 LF。
- 发布门禁为仓库测试 `33/33`、`open_kaishek` 三个生产入口均 `VALIDATED`，简体中文实机结论见 `docs/iteration-2-acceptance-report.md`；其他 9 种语言仅静态校验通过。
