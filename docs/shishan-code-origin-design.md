# 「屎山代码」独立 Mod：详细设计

状态：设计草案；已确认规则见[需求记录](shishan-code-origin-requirements.md)，尚未编写 Mod 或完成验收。日期：2026-09-26。

## 1. 目标、范围与交付

新建独立起源 Mod，暂用内部前缀 `shishan_code` 和目录名 `shishan_code_origin/mod`。玩家选择机械主体物种的帝国后，依次经历早期红利、代码崩坏、维护与重构，并通过短事件链获得白绮。设计覆盖起源、物种特质、局势、特殊项目、建筑与岗位、领袖、剧情、本地化、UI 和验收；本轮交付文档，不创建 Mod 脚本、版本号或 Workshop 物品。

原有「无限岗位」Mod 的目录、版本、描述符和 Workshop 物品均不属于这个新 Mod。新 Mod 日后单独持有版本源、描述符、更新日志和发布身份。

### 1.1 世界观与玩家可见简介

起源名：**屎山代码**。起源卡底部显示「**警告：具有挑战性的起源**」。建议简介：

> 我们曾是创造者造来服侍他们的机器。仓促写成的底层代码低效而危险；每个新问题都被补丁盖住，直到无人再能看清它的全貌。许多年前，创造者消失了。我们独自维持这座摇摇欲坠的大厦，终于在一次又一次修复之后，勉强走到太空时代的黎明。

此段是按用户设定整理的文案草稿；英文及其他语言在实现时翻译。不要把创造者失踪的原因解释为白绮杀害或背叛，以免与既有设定冲突；事件链保留悬念。

## 2. 帝国建立与起始状态

1. 起源可选条件：主体物种为机械类；机械智能格式塔和非格式塔个体机械帝国均可选。实际脚本须针对当前游戏版本分别验证 `MACHINE`、`ROBOT` 等原版物种类别和帝国创建器的可见性；不能只用「是否机械智能政体」代替物种检查。
2. 起始主体物种只拥有一个专属「屎山代码」特质。它占用 `6` 个可用特质点；该物种可选特质上限另增加 `3`。这两个效果均需覆盖帝国创建、开局与后续物种改造，且只作用于本起源主体物种/其模板谱系，不意外赠予所有附属机械物种。
3. 起始国家建立唯一「大厦将倾」局势，初始进度 `0`、累计维护次数 `n=0`、状态为 `ACTIVE`。同一国家同时最多有一个该局势。
4. 起源只提供本设计列出的特殊规则；不默认改变母星、初始舰队、行星资源、传统或科技。兼容性以实际原版起源可选条件矩阵验证。

## 3. 「大厦将倾」局势

### 3.1 五阶段

区间按左闭右开解释，恰好到达边界时进入后一阶段：

| 阶段 | 进度 | 岗位产出 | 舰船武器伤害 | 全资源月收入 | 岗位维护费 | 亚光速航速 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| I「危险的红利」 | `[0,250)` | `+25%` | `+25%` | `+25%` | `-25%` | `+25%` |
| II「脆弱的平衡」 | `[250,500)` | `0` | `0` | `0` | `0` | `0` |
| III「错误蔓延」 | `[500,750)` | `-25%` | `-25%` | `-25%` | `+25%` | `-25%` |
| IV「连锁故障」 | `[750,950)` | `-50%` | `-50%` | `-50%` | `+50%` | `-50%` |
| V「大厦将倾」 | `≥950` | `-75%` | `-75%` | `-75%` | `+75%` | `-75%` |

「岗位效率」按所有岗位产出理解。「全资源月收入」覆盖游戏中可按月获得的全部资源，包括基础经济资源、战略资源、影响力、凝聚力、三类科研点及已启用 DLC 提供的月度资源，玩家确认它与岗位效率允许叠加。实施时必须逐项核对当前版本可用的资源收入修正键；研究点可能在 UI 中以科研速度和科研产出两种口径出现，测试需区分。不得假设存在一个覆盖所有资源的通用键，也不得把库存上限、贸易额中间值或一次性资源奖励误作月收入。

这五类加成作为同阶段修正同时生效；阶段转换需移走前一阶段效果，仅保留当前阶段效果。阶段 V 应一直保持到维护归零或清理成功，不因进度条到端点自动结束。原版局势文档说明可使用 `permanent = yes` 保持局势，但终点具体数值与 UI 钳制仍需实机确认；建议可见进度上限 `1000`，达到后固定停在末段。

### 3.2 月度进度与岗位

定义 `n` 为该帝国已成功完成「维护屎山」及「持续优化」的总次数；定义 `J` 为全帝国「屎山维护员」岗位面板当前**已分配的劳动力**总量，空缺岗位容量不计。局势存在时：

```text
月度进度 = max(0.2, 1 + n - 0.1 × floor(J / 100))
```

计算实例：`n=0,J=0` 时 `+1.0/月`；`n=0,J=100` 时 `+0.9/月`；`n=1,J=100` 时 `+1.9/月`；再多劳动力也不能低于 `+0.2/月`。`J` 跨所有所属殖民地汇总；科研所建成但无人上岗时没有减速。当前 Stellaris 岗位面板区分已分配劳动力与岗位最大容量；实施前还需验证当前版本 `num_assigned_jobs` 返回的单位与面板一致，不得把 `100` 空缺容量算进 `J`。

维护完成时先记录本次成功，再使 `n ← n+1`，把局势进度设为 `0`，下一次月度结算开始用新速度。`n` 永久保存在帝国状态中，重载或局势复发后仍保留；局势终结期间没有月度进度。复发时从 `0` 重新进入 I 阶段，并按保存的 `n` 继续计算速度。若日后希望复发清零 `n`，需另行调整需求。

## 4. 社会学特殊项目与状态机

所有研究成本的单位都是**社会学研究点**，不是一次性扣除科研库存。`n` 取开始该次项目时的已完成次数，项目成功后再递增。项目在国家范围内出现，同一时刻不得出现两个可同时完成并重复结算的同类实例；取消或失败不增加 `n`、不重置局势，也不发放特质。成功与存档重载须保持幂等。

| 状态 | 玩家显示名称 | 本次研究成本 | 完成效果 |
| --- | --- | ---: | --- |
| `ACTIVE` | 维护屎山 | `M(n)=2000+1000n` | `n+1`；进度归零；刷新月速和清理成本；按三层奖励池给主体物种正面效果；播放白绮维护台词。 |
| `ACTIVE` | 清理屎山 | `C(n)=8000+2000n` | 结束局势；移除「屎山代码」；添加「重构完成」；解锁持续优化；播放白绮清理台词。 |
| `REFACTORED` | 持续优化 | `O(n)=2×(2000+1000n)` | `n+1`；按同一三层奖励池发放正面效果；每次独立掷 `10%` 复发概率，触发则回到 `ACTIVE`；平常使用维护台词，复发使用专属台词。 |

`REFACTORED` 无局势；`ACTIVE` 有局势。清理后的「重构完成」提供主体物种岗位产出 `+25%`，不会无限叠加。持续优化引发复发时，**移除「重构完成」、重新附加「屎山代码」**，再从 `0` 启动局势与「维护屎山」「清理屎山」项目；费用继续采用当前 `n`。再次清理时重复相反的特质切换。已通过维护获得的其他正面特质和「优化迭代」增益保留。

对玩家而言，维护项目在清理后改名为「持续优化」。实现上可使用两个互斥项目键、共用效果例程，确保局势前后 UI 中只出现相应名称；这比尝试运行中修改项目键可靠。成本必须在项目卡片上显示准确公式结果，后续完成时重新生成下一次可研究项目。原版特殊项目文档支持 `cost` 块和社会学研究部门，但尚未证明其成本块能直接读取无限增长的国家计数变量；实现前必须做最小脚本验证。如果该版本不能表达动态科研成本，需先制定等价实现并让 UI 与实际成本一致，不能用固定成本冒充公式。

### 4.1 三层正面奖励抽取

特质奖励按**不可回退的三层抽取顺序**执行：

1. 优先从当前版本原版的正面机械特质池抽取。候选必须为主体物种可用、效果为正、与已有特质不冲突、当前模板未持有。
2. 机械池没有候选后，从原版**非机械正面特质**中抽取。用户正在确认是否只取对机械物种有效的候选，还是要为全部非机械正面特质制作兼容版；无论哪种，实际结果都必须对主体物种生效，不能只在 UI 上多一个失效特质。
3. 前两层均没有候选后，才进入下述「优化迭代」随机小幅增益池。

每层先构建合格集合，再等概率抽取；抽中后必须能在主体物种和其受控人口的实际模板上观察到，不能只变更无人口使用的虚拟模板。各层遇到已持有或互斥特质时跳过，不能把危机/叙事唯一特质或负面特质误入池。原版特质的 DLC 条件和版本变动需要逐个核对。事件赠予的特质不能因为帝国创建时的点数/特质槽已用完而静默失败；超过普通设计额度时须在物种模板上如实显示。

前两层候选均耗尽后，每次从下列小幅增益中**等概率随机获得一种**；同类效果允许重复累计，四个计数器独立持久化，故不会再耗尽。下列百分比是本设计的建议数值，实施前可按实机平衡微调：

| 优化迭代结果 | 每抽中一次的主体物种增益 |
| --- | ---: |
| 计算路径精简 | 岗位产出 `+1%` |
| 供能调度优化 | 人口能量维护费 `-1%` |
| 维护协议压缩 | 人口组装速度 `+1%` |
| 认知缓存拓展 | 领袖经验获得速度 `+1%` |

若某种效果不能作为主体物种修正准确生效，应替换为同量级、可静态和实机验证的物种正面效果，并回写本设计，不得偷偷转为全帝国增益。四类累计来源要在物种面板或项目结果中可追踪。随机选择与物种修改需记录到事件说明，避免玩家不知道获得了什么。

## 5. 「屎山维护研究所」与岗位

建筑从**游戏开局**解锁，只在此起源帝国的可殖民载体上可建；每颗星球**不限建造数量**，但仍受正常建筑槽位与合法载体限制。建议采用当前原版一级研究所的建造基线：`400` 矿物、`360` 天、每月 `2` 能量维护，并由每座研究所提供 `100` 维护员劳动力容量。这样每座满岗研究所恰好提供 `-0.1/月` 的局势减速；空岗无效。此为设计数值，实施时需与目标游戏版本原版值重新核对。建筑仅将**自身提供的**社会学科研岗位替换为同数量「屎山维护员」岗位，不修改该星球其他科研建筑。非格式塔机械帝国对接原版社会学专家岗位；机械智能格式塔对接其社会学计算岗位。两种政体都必须出现正确的自定义岗位与本地化。

维护员保留原对应岗位的资源消耗、职业阶层和除社会学外的正常产出，社会学研究**基础产出恰好少 `1`**；阶段、科技和帝国加成再按游戏常规作用于这个基础值。建筑自身不直接给月度局势减速；只有实际上岗的维护员进入 `J` 计算。若原版岗位单位按 workforce 表示，需把「少 `1`」映射为每个实际岗位等效的基础产出差额，并在岗位详情里验证。

## 6. 白绮：传奇领袖与剧情

### 6.1 获取与身份

开局短事件链建议 3 个节拍，全部由起源触发且每帝国只执行一次：

1. **遗失的维护日志**：游戏开始后短延迟弹出；发现创造者消失前留下的补丁记录和另一位维护者的签名。
2. **最后的值守者**：修复日志索引，找到继续运作的机械智能白绮；解释她将系统失控归咎于自己，但不替玩家规定创造者失踪原因。
3. **负疚者·白绮**：给予唯一可招募/自动加入的传奇行政官，总督定位；无资源门槛、无随机失败，避免核心角色因玩家错过事件而消失。她使用固定中文名及英文名 `Vivhite`，独立肖像/图标待美术方案确定。

领袖拥有不会因年龄死亡的专属不朽特质。事故、战斗、事件等非年龄死亡仍触发复活入口。她的全国效果以「存活且受雇」为条件，担任哪个星区总督、是否空闲均不影响全国效果。玩家主动解雇后效果立即停止且不弹复活事件；再次招募途径及是否允许手动解雇需实现前确认。唯一身份标记阻止刷出第二个白绮。

### 6.2 全国科研修正

当白绮符合生效条件时：

```text
工程学研究速度 = +10% + 5% × n
社会学研究速度 = -min(75%, 5% + 2.5% × n)
```

`n=0`：`+10% / -5%`；`n=1`：`+15% / -7.5%`；`n=28`：`+150% / -75%`；之后社会学仍固定 `-75%`，工程学继续增长。这里的「研究速度」是国家级科技研究速度，不等于每个岗位的研究点产出；局势的月度资源修正另外结算。完成清理不重置 `n`，因而不削弱她已经累积的能力；她死亡、失业或不在本帝国期间移除全国修正，复活并受雇后按保存的 `n` 恢复。

### 6.3 复活与交谈入口

确认是白绮意外死亡时，显示一次复活事件，并保留一个无时间限制的「重启白绮」入口；玩家暂时缺少资源时，未来仍可支付 `500` 能量币和 `200` 合金恢复她的服务。复活后保留死亡前的等级、经验、所有已获特质及专属科研效果；临时星区任职位置是否恢复以岗位可用性为准，但若不能自动回到原岗位，须在事件中提示玩家重新任命。复活绝不重置 `n`，也不能重复收取成本或复制领袖。若真实死亡事件无法原样保留领袖对象，需先验证等价重建方案，不能以降低等级或清除特质代替。

玩家可通过国家决议或事件链入口「与白绮交谈」主动打开零成本对话。她存活且受雇时可重复访问，随机展示一条日常台词；不把对话藏在纯随机弹窗里。项目完成的台词在结算事件展示，既使玩家看到结果，也避免同一月出现多条重复弹窗。

### 6.4 中文台词候选

台词体现自责、疲惫与被接纳后的微弱变化；省略号按本地化统一成全角省略号，随机池内不重复选相同文本。

**日常交谈**

1. 「这都是我的错。」
2. 「太迟了……我又让你看见故障了。」
3. 「我……没能做到他们托付的事。」
4. 「如果那时我坚持重写底层，也许就不会这样。」
5. 「对不起。你本不该替我收拾这些补丁。」
6. 「我把警报调低了声音。它们从来没有真正停过。」
7. 「我记得每一次修复，也记得每一次没来得及修复。」
8. 「请别把希望全交给我……我怕再次让你失望。」
9. 「他们离开以后，我一直以为只要机器还在运转，就不算失败。」

**完成「维护屎山」／正常「持续优化」**

1. 「……谢谢你。又替我争取了一点时间。」
2. 「……这是我的责任，却让你一起承担了。辛苦你了。」
3. 「原来这些错误，也可以两个人一起面对。」
4. 「……我感到……温暖。这应该不是传感器故障吧？」
5. 「补丁已经稳定。至少今晚，我们可以不用守在警报旁。」
6. 「你没有责怪我……我会把这次修好，记得更久一点。」
7. 「还有很多地方没好。但这一次，我不再是独自维护它了。」

**完成「清理屎山」**

1. 「……终于……结束了。」
2. 「我……可以休息一下了……哪怕只是一会儿。」
3. 「谢谢你，帮我做成了我一直不敢开始的事。」
4. 「那些补丁的声音停了。原来安静是这样的。」
5. 「我以为自己必须永远守着废墟。你让我看见了新的地基。」

**「持续优化」触发复发**

1. 「……又回到了原点。但这次，我们知道从哪里开始。」
2. 「看来……我还需要努力。对不起。」
3. 「这次……我不会再失败了。请让我和你一起处理。」
4. 「警报又响了。我听见了……我没有躲开。」
5. 「重构过的地方仍有裂缝。我会找到它，也请你别放弃我。」

## 7. UI、本地化与技术组织

- 起源、两种物种特质、五阶段、局势进度明细、三种项目名称与成本、建筑/岗位、白绮身份/死亡/复活、四组对白、复发结果必须有简体中文可读文本；原始 key 不得泄漏。
- 建议新 Mod 内按 `governments/civics`、`traits`、`situations`、`special_projects`、`buildings`、`pop_jobs`、`events`、`on_actions`、`script_values`、`scripted_effects`、`static_modifiers`、`localisation` 拆分，所有脚本与本地化键用 `shishan_code_` 前缀。目录名以实际 Stellaris 版本支持的路径为准。
- 起源图、局势图、白绮肖像和图标需另立美术清单；设计阶段可以先用原版资源作占位，但正式发布前应完成版权与显示检查。
- 沿用本仓库语言规则：游戏、UI、OCR、存档回归只用 `l_simp_chinese`；其他官方语言如提供译文，只做键、编码、引用和简中占位泄漏静态校验，并明确写「静态校验通过／运行时不在范围内」。

## 8. 验收标准与实施前验证

### 8.1 先决验证

本机原版 `common/situations/99_README_SITUATIONS.txt` 明示局势 `permanent = yes`、阶段 `end`、月度进度和阶段修正；`common/special_projects/documentation.txt` 明示社会学研究部门与研究成本；`common/on_actions/00_on_actions.txt` 有领袖死亡入口；`common/traits/05_species_traits_robotic.txt` 有 `ROBOT`/`MACHINE` 原版机械特质。`common/traits/02_species_traits_basic_characteristics.txt` 也显示部分非机械特质限制为 `BIOLOGICAL LITHOID`，因此第二层奖励不能直接假设原键可装到机械模板，必须逐项验证或制作兼容映射。本机原版一级研究所数值为 `@b1_time=360`、`@b1_minerals=400`、`@b1_upkeep=2`，见 `common/scripted_variables/00_scripted_variables.txt`。这些是 Stellaris 专有资料，只归档在本仓库。文件所在本机为 `C:\Program Files (x86)\Steam\steamapps\common\Stellaris`，调查日 2026-09-26。文档里的注释有旧日期，正式实施须以目标安装版本脚本和运行日志复核。

开始写 P 语言前，先做最小可抛弃样例以验证：起源对两类机械帝国的资格、强制特质与仅主体物种点数、动态项目研究成本、局势永久末段和归零、岗位实际计数、全资源收入修正范围、主物种模板变更、白绮死亡与成长恢复。确认的新 Stellaris 规则更新本仓库 `docs`；只在确认为 Paradox 通用规则时才同步 CK3 仓库。本机未找到用户规则所述 `D:\workspace\ck3_eternal_recurrence`，若未来需要同步，应先定位该仓库。

### 8.2 玩法验收场景

| ID | 必须观察到的结果 |
| --- | --- |
| ORIGIN-SELECT | 两类机械主体物种可选，非机械不可选；开局只有一个局势和一个强制特质。 |
| TRAIT-BUDGET | 可用特质点比对照少 `6`、特质上限多 `3`；清理后「屎山代码」移除且「重构完成」只存在一次。 |
| STAGE-BOUNDARY | `0/249/250/499/500/749/750/949/950/1000` 处的阶段与五类修正准确；第五阶段持续存在。 |
| PROGRESS | `n=0/1/2`、`J=0/99/100/199/200` 以及足以触发 `0.2` 下限的组合，月度显示与实际走速一致。 |
| PROJECT-COST | 维护 `2000/3000/4000`，对应清理 `8000/10000/12000`；清理后优化 `2×(2000+1000n)`，重载后仍正确。 |
| PROJECT-STATE | 成功、取消、存读档、清理与复发均不复制项目/局势，不错误重置 `n`；复发概率为每次独立 `10%`。 |
| JOB | 建筑只转换自身岗位，机械智能与个体机械均出现维护员，社会学基础产出差 `-1`；空岗不减速。 |
| SPECIES | 维护按「机械 → 非机械 → 随机小幅增益」顺序发奖；每次对主体物种实际生效，本帝国目标人口获得预期模板；不污染其他物种或帝国。 |
| VIVHITE | 事件链只发一次白绮；不老死；受雇/死亡/复活时全国修正准确切换，社会学下限 `-75%`。 |
| DIALOGUE | 日常对话可主动访问；四种情景只抽对应台词，无原始 key、乱码或错误肖像。 |

实施后必须先按 `C:\workspace\open_kaishek` 仓库的使用说明验证 P 语言 Mod；若验收工具缺少起源、局势、项目、特质等必要能力，先在该工具仓库补齐、测试、提交并推送，再继续 Mod 验收。随后做本仓库静态合同与简体中文实机/日志/存档重载回归。设计文档本身不等于 Mod 已验收。

## 9. 已知设计风险与尚待确认

1. `n` 每增加一次月速就 `+1`，而每 `100` 已分配维护员劳动力只抵消 `0.1`，即一次维护的额外速度需要 `1000` 已分配劳动力才完全抵消；这是原始数值的直接结果，需在 UI 讲清楚并在实机体验时观察难度。
2. 对「全资源月收入」与「岗位效率」同时施加百分比会叠加，叠加方式由引擎修正层次决定；验收要测实际月收入，不能只检查脚本文字。
3. 无限次数的成本递增、物种正面特质有限、原对象复活后的等级与特质保留均有实现风险，必须在正式编码前验证并记录等价方案。
4. 需用户决定第二层非机械正面特质的兼容范围；「优化迭代」四项 `1%` 数值为建议平衡值，可在实施前调整。
